{
  "name": "TBD",
  "private": true,
  "version": "0.1.0",
  "description": "Web UI for Claude Code — manage sessions from your browser",
  "type": "module",
  "bin": {
    "TBD": "./bin/cli.js"
  },
  "files": [
    "dist/",
    "public/",
    "bin/"
  ],
  "scripts": {
    "build": "tsc",
    "build:ui": "cd ui && npm ci && npm run build",
    "prepublishOnly": "npm run build:ui && npm run build",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.45",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
