SSH_PASSWORD=changeme
API_PASSWORD=changeme

# Optional: Tailscale VPN (get auth key from https://login.tailscale.com/admin/settings/keys)
# TS_AUTHKEY=tskey-auth-xxxxx
# TS_HOSTNAME=hatchpod

# Optional: Dotfiles repo (cloned to ~/dotfiles on first boot)
# DOTFILES_REPO=https://github.com/youruser/dotfiles.git
# DOTFILES_BRANCH=main
